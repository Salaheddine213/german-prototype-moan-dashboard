fastapi>=0.115
granian[reload]>=1.6
duckdb>=1.2
pydantic>=2.0
pydantic-settings>=2.0
# pandas 3.x requires pytz for tz-aware datetime handling.
pytz>=2024.1
pandas>=2.2
pyarrow>=18.0
openpyxl>=3.1
scipy>=1.14
numpy>=1.26
# Used by /api/ml feature-importance to load the production LightGBM Booster.
lightgbm>=4.0
# Required by FastAPI's TestClient (and useful for any HTTP client work).
httpx>=0.27
# Test runner for the backend test suite.
pytest>=8.0
